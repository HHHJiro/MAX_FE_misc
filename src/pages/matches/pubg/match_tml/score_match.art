{{set match_list = $data}}
{{if match_list.length == 0 }}
  <li>排名总表</li>
{{else}}
  {{ each match_list match index}}
    <li>
    {{ if index == 0 }}
    排名总表
    {{ else }}
    {{ match.desc }}
    {{/if}}
    </li>
  {{/each}}
{{/if}}